{
  "name": "cloudflare-auth-test",
  "version": "1.0.0",
  "description": "Simple authentication test using Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {
    "itty-router": "^4.0.23"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.26.0"
  }
}
